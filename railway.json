{
    "build": {
        "builder": "NIXPACKS"
    },
    "deploy": {
        "startCommand": "composer dump-autoload && php artisan migrate --force && php artisan config:cache && php artisan serve --host=0.0.0.0 --port=$PORT"
    }
}
